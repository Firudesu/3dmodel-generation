services:
  - type: web
    name: 3d-model-generator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start.py
    plan: starter  # Use starter plan with more memory